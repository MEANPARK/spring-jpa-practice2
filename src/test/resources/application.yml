spring:
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/jpashop
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#    jpa:
#      hibernate:
#        ddl-auto: create #자동으로 테이블 만듦(drop 후 create)
#        #spring.jpa.hibernate.ddl-auto: create
#        #이 옵션은 애플리케이션 실행 시점에 테이블을 drop 하고, 다시 생성한다.
#      properties:
#        hibernate:
#          #        show_sql: true #spring boot Guide 확인
#          format_sql: true  #spring boot Guide 확인

#  datasource:
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop #만들고 종료할 때 drop
#        #spring.jpa.hibernate.ddl-auto: create
#        #이 옵션은 애플리케이션 실행 시점에 테이블을 drop 하고, 다시 생성한다.
#    properties:
#      hibernate:
##        show_sql: true #spring boot Guide 확인
#        format_sql: true  #spring boot Guide 확인

logging.level:
  org.hibernate.SQL: debug #hibernate가 생성하는 sql이 보인다(logger를 통해)
#  org.hibernate.orm.jdbc.bind: trace

